[pytest]
markers =
    real_time: Test cases that runs in real time and therefore are slower than others.
    integration: Test cases that checks integration of a few units (e.g. functions, methods).
    functional: Test cases that checks whole features (e.g. modules, packages).